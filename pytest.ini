[pytest]
markers =
    performance: marks tests as performance tests (deselect with '-m "not performance"')
    slow: marks tests as slow (deselect with '-m "not slow"')
    s3: marks tests that require S3/MinIO integration

testpaths = tests

# Performance tests can be skipped in CI with: pytest -m "not performance"
